linters:
  enable-all: true
  disable:
    - gosmopolitan
    - wsl
    - forbidigo
    - gochecknoglobals
    - depguard
    - godox
    - exportloopref # deprecated
    - gomnd # deprecated
    - execinquery # deprecated

    # on hold
    - gochecknoinits
    - exhaustruct

linters-settings:
  varnamelen:
    ignore-decls:
      - t testing.T
      - e error

issues:
  exclude-rules:
    - path: _test.go
      linters:
        - funlen
